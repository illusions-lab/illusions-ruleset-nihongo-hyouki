/**
 * The sample-fw-exclaim rule has been replaced with nh-* rules.
 * Golden example and rule-specific tests are in nh-rules.test.ts.
 */
import { describe, it } from "vitest";

describe("placeholder (sample rule removed)", () => {
  it("nihongo-hyouki ruleset uses nh-* rules — see nh-rules.test.ts", () => {
    // intentionally empty
  });
});
