{
  "name": "illusions-ruleset-nihongo-hyouki",
  "version": "0.4.1",
  "private": false,
  "description": "日本語表記（日本エディタースクール）に基づく illusions 校正ルールセット。",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsup",
    "build:min": "tsup --minify",
    "validate": "node scripts/validate.mjs",
    "check": "npm run validate && npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.9"
  }
}
